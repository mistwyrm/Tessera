namespace Tessera.Utilities
{
    internal class Constants
    {

        /// <summary>
        /// A DataObject format used to store ListBoxItems
        /// </summary>
        public const string ListBoxItemFormat = "list-box-item-format";

        /// <summary>
        /// A DataObject format used to store the source of a drag event
        /// </summary>
        public const string DragSourceFormat = "drag-source-format";

        /// <summary>
        /// The square distance the pointer can be moved before triggering a drag
        /// </summary>
        public const double DragDistance = 200;
    }
}
